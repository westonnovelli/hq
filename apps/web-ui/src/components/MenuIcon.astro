---
interface Props {
    height?: number;
    width?: number;
    class?: string;
}

const { height = 24, width = 24, class: className } = Astro.props;
---
<svg xmlns="http://www.w3.org/2000/svg" height={`${height}px`} viewBox="0 -960 960 960" width={`${width}`} fill="currentColor" class={className}>
<path d="M120-240v-60h720v60H120Zm0-210v-60h720v60H120Zm0-210v-60h720v60H120Z"/>
</svg>
