---
import BaseLayout from './base-layout.astro';
import type { routes } from '../../routes.ts';

interface Props {
    location: routes;
    title: string;
}

const { location, title } = Astro.props;
---
<BaseLayout location={location} title={title}>
    <slot/>
</BaseLayout>

